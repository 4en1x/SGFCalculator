import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    bottomButton: {
        borderRadius: 20,
        alignItems: 'center',
        backgroundColor: '#1b4567',
    },

    textContainer: {
        color: '#ffffff',
        alignItems: 'center',
        fontSize: 13,
    },
});
